#include <bits/stdc++.h>
using namespace std;

int jump(vector<int>& nums) {
        
}

int main() {
    int n;
    cin>>n;
    vector<int> v(n);
    for(int i=0;i<n;i++) {
        cin>>v[i];
    }
    cout<<jump(v)<<endl;
    return 0;
}